# weave has no public HTTP surface. All clients (kiln, telegram-bot,
# explorer, dashboard) reach it on the docker network as `weave:8007`.
# The gateway's render step requires this file to exist for every
# enabled service; we leave it empty so Caddy doesn't try to provision
# a TLS cert for a hostname with no DNS.
