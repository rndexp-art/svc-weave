# Per-service env vars. The gateway loads the parent repo's .env at compose
# time, so any vars referenced from compose.fragment.yml as ${NAME_FOO} must
# be present there.
#
# Example:
# weave_FOO=bar
